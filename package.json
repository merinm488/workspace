{
  "name": "workdeck",
  "version": "1.0.0",
  "description": "Workdeck — a unified landing to access daily tools",
  "type": "module",
  "scripts": {
    "dev": "node server/dev-server.js",
    "start": "node server/dev-server.js",
    "build:docs": "cd docs-src && npm install && npm run build",
    "build": "npm run build:docs && node scripts/build-dist.mjs",
    "postinstall": "cd docs-src && npm install"
  },
  "keywords": [
    "workdeck",
    "docs",
    "spreadsheet",
    "sheets"
  ],
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
