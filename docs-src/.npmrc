# Speed up CI installs (Vercel builds): skip registry audit/funding lookups
# and trust the local cache instead of revalidating every package online.
# Lives here (not just repo root) because npm only reads .npmrc from the
# nearest package.json directory — root config doesn't cascade into `cd docs-src`.
audit=false
fund=false
prefer-offline=true
